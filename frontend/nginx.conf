server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location /healthz {
        default_type text/plain;
        try_files /healthz =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
